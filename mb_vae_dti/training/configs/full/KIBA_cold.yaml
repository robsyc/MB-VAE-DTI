model:
  checkpoint_path: "data/results/full/pretrain/cold/full_pretrain_cold/checkpoints/best_model.pt"

training:
  learning_rate: 0.0001

data:
  batch_size: 32
  
logging:
  experiment_name: "full_KIBA_cold"
  save_dir: "data/results/full/KIBA/cold"

gridsearch:
  training.learning_rate: [0.0001, 0.0005, 0.001]
  data.batch_size: [16, 32, 64]